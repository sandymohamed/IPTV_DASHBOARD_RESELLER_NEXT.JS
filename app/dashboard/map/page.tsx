import { Box, Typography } from '@mui/material';

export default function MapPage() {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 5 }}>
        Map Connections
      </Typography>
      <Typography variant="body1">Map connections visualization page...</Typography>
    </Box>
  );
}
